<div class="container">
  <div class="row">
  <h2>Users</h2>
    <% @users.each do |user| %>
      <div class="border">
        <%= link_to user.name, "/users/#{user.id}", :style => "font-size: 1.5rem;" %>
      </div>
    <% end %>
  </div>
</div>