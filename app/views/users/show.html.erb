<h1>ユーザー情報</h1>
<br>
<div>
  <h2><%= @user.name %></h2>
  <p><%= @user.email %></p>
</div>

<% if @user.id == @current_user.id %>
  <div class="row mt-3">
    <div class="col-1">
      <%= link_to "編集", "/users/#{@user.id}/edit", {class: "btn btn-outline-dark"} %>
    </div>
  </div>
<% end %>
