<h1>とりま本リスト</h1>
<% if !@records.empty? %>
  <div class="row mt-5">
    <div class="col-2">
      <%= link_to "お気に入り", "/users/#{@current_user.id}/favorites", {class: "btn btn-outline-danger"} %>
    </div>
    <div class="col-2">
      <%= link_to "新規登録", "/records/new", class: "btn btn-outline-dark" %>
    </div>
  </div>
  <br>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">タイトル</th>
        <th scope="col">登録日時</th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @records.each do |record| %>
        <tr>
          <td scope="col" class="align-middle"><%= link_to record.title, "/records/#{record.id}", {class: "link-dark"} %></td>
          <td scope="col" class="align-middle"><%= record.created_at.to_fs(:datetime_jp) %></td>
          <td scope="col"><%= link_to "編集","/records/#{record.id}/edit", {class: "btn btn-outline-dark"} %></td>
          <td scope="col"><%= button_to "削除", "/records/#{record.id}/destroy", {class: "btn btn-dark"} %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <% else %>
  <br>
  <div class="d-flex align-items-center justify-content-center mb-5" style="height: 20%;">
    <h2>とりあえず読んだ本を登録してみよう！</h2>
  </div>
  <br>
  <div class="d-grid gap-2 col-3 mx-auto">
    <%= link_to "新規登録", "/records/new", {class: "btn btn-outline-dark"} %>
  </div>
  <% end %>
</div> 
