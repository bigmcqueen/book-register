<h1>Records#show</h1>
<p>Find me in app/views/records/show.html.erb</p>
<div>
  <p><%= @record.thoughts %></p>
  <p>created_at: <%= @record.created_at %></p>
</div>
<div class="row">
  <div class="col-1">
    <%= link_to "編集","/records/#{@record.id}/edit", {class: "btn btn-primary"} %>
  </div>
  <div class="col-1">
    <%= button_to "削除", "/records/#{@record.id}/destroy", {class: "btn btn-primary"} %>
  </div>
</div>