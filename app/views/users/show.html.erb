<div class="container">
  <div class="row">
    <div class="border">
      <%= image_tag "/user_images/#{@user.image_name}", size: '80x80' %> 
      <h2><%= @user.name %></h2>
      <p><%= @user.email %></p>
      <%= link_to "編集", "/users/#{@user.id}/edit" %>
    </div>
  </div>
</div>